const prompt = require('prompt-sync')();

//8-verifique se a letra digitada é vogal
letra = parseInt(prompt("Digite uma letra: "));

if () {
    console.log(" - É uma vogal");
} else {
    console.log(" - Não é uma vogal");
}